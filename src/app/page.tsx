import ModeSelector from '@/components/ModeSelector'

export default function Home() {
  return (
    <main className="min-h-screen bg-zinc-50 flex items-start justify-center">
      <ModeSelector />
    </main>
  )
}
